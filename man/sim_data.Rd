% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_data.R
\name{sim_data}
\alias{sim_data}
\title{Simulate Data for Diagnostic Plots}
\usage{
sim_data(
  n = 2000,
  type = c("quadratic", "heteroscedastic", "gumbel", "proportionality", "interaction"),
  ...
)
}
\arguments{
\item{n}{Integer specifying the number of observations to simulate. Default is
\code{2000}.}

\item{type}{Character string specifying the type of data/model to simulate.
Default is \code{"quadratic"} (linear relation with a quadratic trend). Other options
include: \code{"heteroscedastic"} (non-constant variance), \code{"gumbel"} (Gumbel
error distribution), \code{"proportionality"} (non-proportional hazards), and
\code{"interaction"} (interaction effect).}

\item{...}{Additional optional arguments (currently ignored).}
}
\value{
A data frame containing the simulated predictor(s) and the ordered
factor response variable \code{y}.
}
\description{
Generate simulated data sets designed to illustrate various model
misspecifications and diagnostics for ordinal regression models, as
described in Liu and Zhang (2017).
}
\examples{
# Simulate quadratic data
set.seed(101)
df <- sim_data(n = 500, type = "quadratic")
head(df)
}
\references{
Liu, Dungang and Zhang, Heping. Residuals and Diagnostics for Ordinal
Regression Models: A Surrogate Approach.
\emph{Journal of the American Statistical Association} (accepted).
}
